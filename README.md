# laravel-docker-sail-redis-mysql

curl -s "https://laravel.build/laravel-9-course?witf=mysql,redis,mailhog" | bash

nano ~/.bashrc

alias sail='[ -f sail ] && bash sail || bash /caminho-do-projeto/vendor/bin/sail'

